let eventsClass = document.getElementsByClassName("events")

eventsClass.innerHTML = <%= j render "events", events: @events %>


// selectionner la div .events
// innerHTML(<%= j render "events", events: @events %>)
