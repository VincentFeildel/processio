<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Leasy - Automatisez votre gestion locative</title>
    meta_description: "Relevant description"
    <link rel="icon" href="http://example.com/favicon.png">
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= favicon_link_tag %>
  </head>
  <body>
    <div class="page-framing">
      <%= render 'shared/nav_bar' unless !user_signed_in? %>
    </div>
    <div class="content-page">
      <p class="notice hidden"><%= notice %></p>
      <p class="alert hidden"><%= alert %></p>
      <%= yield %>
    </div>

    <%= javascript_include_tag 'application' %>
  </body>
</html>
